import { AosDirective } from './aos.directive';

describe('AosDirective', () => {
  it('should create an instance', () => {
    const directive = new AosDirective();
    expect(directive).toBeTruthy();
  });
});
